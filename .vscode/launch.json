{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run bun dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
